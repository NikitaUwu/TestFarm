# Server only. Set NEON_BASE when DATABASE_URL belongs to another integration.
NEON_BASE=
DATABASE_URL=
BLOB_READ_WRITE_TOKEN=
TSARROUTER_API_KEY=
TAVILY_API_KEY=
INTERNAL_API_TOKEN=
# Stable application URL for same-deployment Python and Rules calls.
APP_ORIGIN=
# Optional model/channel overrides, still subject to the zero-price policy.
TSAR_MODEL=
TSAR_CHANNEL=
TSAR_STT_MODEL=
TSAR_STT_CHANNEL=
# Only for one-way import; do not add legacy credentials to Vercel.
LEGACY_DATABASE_URL=
