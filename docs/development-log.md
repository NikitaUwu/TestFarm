# Журнал разработки

## 2026-09-08 — начало реализации

- Request: выполнить МастерПромпт.md после подготовки AGENTS.md и четырёх skills.
- Agent plan: вертикальные срезы A10, начиная с persistence и карточки; далее STT, очередь, реальные цепочки, расчёт, доказательства, отчёт, решение и MVP, мониторинг и эксплуатация.
- Модель: текущая модель Codex; точный идентификатор runtime не предоставлен. Инструменты: PowerShell, apply_patch, Docker, браузер, image_gen; дополнительные агенты не использовались.
- Решения: Next.js + FastAPI + PostgreSQL, локальное MinIO, LangGraph worker, отдельный Runner и rules service. Первый поддерживаемый MVP — один сценарий классификации обращений; окончательное создание зависит от решения пользователя в продукте.
- User clarification: локальная Ollama; стандартный endpoint на момент проверки не ответил, адрес уточняется.
- Внешние ручные настройки: отсутствуют. Docker Desktop запущен агентом.
- Implementation/review/correction: заполняются по результатам выполненных проверок; наличие файла не считается доказательством работоспособности.

## Источники технических решений

- https://nextjs.org/docs/app/getting-started/installation
- https://docs.langchain.com/oss/python/langgraph/persistence
- https://www.psycopg.org/psycopg3/docs/basic/transactions.html

Обращение: 2026-09-08. Ограничения конкретных провайдеров проверяются отдельно.

## 2026-09-08 — последовательные уточнения провайдера и проверка

- User review/change: Ollama фактически не установлена. По запросу пользователя подготовлен локальный OpenAI .env; присланный в чат секрет не использовался, запрошена локальная замена. Прямой OpenAI вернул 429 при генерации.
- User change: перейти на OpenRouter с ключом из openrouter.txt. Выполнен полный реальный текстовый прогон, но STT вернула 402. Затем пользователь ограничил работу бесплатными моделями при отрицательном балансе. FREE_MODE_ONLY запретил платные модели и поисковые плагины; бесплатный текст работал, бесплатное аудио вернуло 402/403. Подготовлен необязательный локальный faster-whisper/base.
- Последний request: вместо OpenRouter использовать Groq Cloud Free, ключ в GrokKey.txt. Plan: отдельный ProviderAdapter, новый снимок ProviderProfile, блокировка прежних провайдеров, реальные текст/STT/сквозные проверки без платного fallback.
- Implementation: GroqAdapter, ProviderProfile v5, GROQ_API_KEY только в серверном окружении, старые provider credentials убраны из Compose, GrokKey.txt исключён из Git. Ограничены выдержки источников, введены ограниченные повторы явно отклонённых 429; сетевые ошибки POST не повторяются. Платные инструменты и service tiers не подключались.
- Agent review: 26 unit-тестов, TypeScript/production builds, реальный Groq chat/structure/STT и полный ResearchRun (13 вызовов, 12 наблюдений). Повтор после редакционной правки использовал прежние эксперименты; бизнес-наблюдение обновило расчёт/отчёт. Проверены аудио/версии, команды, архив и удаление БД/S3. Подробные ID и условия — verification.md.
- Corrections: строгая JSON Schema для совместимых результатов; сохранение безопасных путей ошибок валидации; нормализация NUL в HTML перед JSONB; устранение пересчёта независимых шагов; исходное аудио сохраняется при отказе STT; демо-baseline явно помечен в UI. Счёт за разработку и смета не составлялись.
- Deployment: все процессы, включая production Web, теперь запускаются Docker Compose. Внешняя публикация не выполнялась. Документация актуализирована и доступна внутри приложения. Локальное предоставление ключей пользователем — единственная настройка аккаунтов; внешняя консоль агентом не использовалась.
- Результат review: интеграция Groq работает, полная приёмка мастер-промпта остаётся открытой по перечисленным в verification.md ограничениям. Демо-прогон не считается доказательством продуктового эффекта и не разрешает автоматический MVP.

## 2026-09-09 — продолжение реализации и эксплуатационная проверка

- Request: продолжить мастер-промпт с Groq Cloud Free; устранить недоступность API/PostgreSQL и проверить новые экраны после входа Reviewer.
- Agent plan: восстановить локальные сервисы, закончить роли, измерения полного процесса, изолированную генерацию MVP; проверить реальные вызовы, восстановление данных и процессов, актуализировать карту требований. Применены четыре project-local skills; их процедуры и постоянные инструкции не копировались в реализацию документации.
- Implementation: отдельные результаты 13 функций, RoleSet v2; серверный таймер с dataset/task/provenance и EffectModel v2; типизированный GeneratedProgram с чистым JavaScript в QuickJS, отдельными Worker/Runner/Rules, очередью сборки и сохранением результатов. SQLite Runner сохраняет идемпотентные запросы; отдельный дочерний процесс обеспечивает kill/timeout, proxy ограничивает выход api.groq.com:443. Rules публикует корректные схемы классификации и типизированного результата, версия 1.1.0.
- Integration corrections: временный HTTP 403 Groq исчез при повторной проверке. Стандартный Compound давал 413, базовая версия 2025-07-23 прошла проверку. PromptSet v7/ProviderProfile v7 используют сохранённый английский запрос, полученный отдельным вызовом; исходный transcript служит запасным контекстом. Ссылки извлекаются только из executed_tools. Исправлена схема карточки: все поля должны явно присутствовать. Платный fallback и смена тарифа не выполнялись.
- Runtime corrections: Worker проверяет доступность провайдера только перед оставшимися ИИ-этапами, поэтому расчёт/отчёт восстанавливаются без Groq. Закрываются SQLite connections. Измеренные фазы одинаково учитываются в основном расчёте, сценариях и sensitivity; demo не подменяет measured. MVP отклоняет неправильный ввод до Runner и не допускает приёмку невалидной/остановленной сборки.
- Agent review: 34 unit-теста; TypeScript и production builds; полный автоматический поиск/исследование — 15 вызовов, 12 наблюдений, 12 ролей до реализации. Отдельный живой тест структурирования подтвердил problem/audience, исходный transcript и отсутствие непрошеного сохранения версии. Генерация сценария заказа и два реальных исполнения дали 360/150. Дополнительная проверка в восстановленной БД прошла через API decision → mvp_jobs → отдельный Worker → implementation_agent → два результата → API accept. Искусственные report/decision явно отмечены как технические предусловия, не решение пользователя.
- Recovery review: восстановлены и сравнены 47 таблиц PostgreSQL, три S3-артефакта и две записи SQLite Runner. Исправлена ошибка PowerShell при создании временного MinIO env; успешный повтор не перезаписывал рабочую БД. SIGKILL Runner сохранил replay. Worker после SIGKILL восстановил расчёт без ключа и новых вызовов; отдельный экспериментальный run завершён со второй попытки с 12 вызовами и 12 наблюдениями. Точное прерывание незавершённого внешнего запроса этим замером не доказано.
- User/agent UI review: во время согласованного backup API был временно остановлен, что вызвало сообщение пользователя о недоступности; после восстановления вход Reviewer выполнен. Проверены отчёт и демотаймер 7,019 секунды, появление новой версии отчёта, ширина 390 px без общей горизонтальной прокрутки. Реальный микрофон пользователя и продуктовая приёмка остаются открытыми.
- Tools/models: PowerShell, apply_patch, Docker Compose, Python unittest/httpx/FastAPI TestClient, браузер CUA, официальная документация Groq. Модель агента — текущая модель Codex, точный runtime ID не фиксировался. Прикладные модели: openai/gpt-oss-20b, whisper-large-v3-turbo, groq/compound-mini. Дополнительные агенты не использовались; ключи не выводились. Новых ручных настроек внешних аккаунтов и внешних публикаций не было.
- Result: API/Worker обновлены проверенными исправлениями, локальный сервис доступен. Реестр и карта владельцев обновлены в requirements.md, результаты и границы приёмки — verification.md. Реальные бизнес-измерения, обоснованная рекомендация и человеческое решение/приёмка MVP не подменены техническими фикстурами.

## 2026-09-10 — публикация исходников в GitHub

- Request: отправить проект в https://github.com/NikitaUwu/TestFarm.git.
- Plan: проверить удалённые ветки, исключения секретов и локальных данных, подготовить первоначальный коммит и отправить main без force push.
- Implementation: создан локальный Git-репозиторий, origin указывает на заданный адрес. Включены код, конфигурации без секретов, миграции, тесты, документация, AGENTS.md и четыре локальных skill. .env, файлы ключей, .local, зависимости и результаты сборки исключены через .gitignore.
- Review: перед публикацией проверяется подготовленное содержимое на форматы ключей и совпадения со значениями локальных секретов; после push проверяется совпадение локального HEAD и origin/main. Приложение не изменялось, повторные функциональные тесты для публикации исходников не требуются. Инструменты: Git, PowerShell, apply_patch; внешняя консоль и дополнительные агенты не использовались.
- Ограничение комплекта: исходный МастерПромпт.md отсутствует в текущем каталоге. Ссылки на него сохранены в постоянных инструкциях; отсутствующий оригинал не воссоздавался по памяти.

## 2026-09-10 — исправление установки зависимостей Vercel

- Request: пользователь предоставил лог неуспешного deployment 07efaad: Corepack не обнаружил packageManager, pnpm проигнорировал несовместимый lockfile, загрузка завершилась ERR_INVALID_THIS.
- Plan: исключить неявный выбор старого pnpm и проверить установку/сборку на чистой копии web, затем отправить исправление в тот же GitHub-репозиторий.
- Implementation: pnpm 10.34.5 закреплён в корневом package.json, web/package.json и Dockerfile; Node.js 22.x объявлен в engines. web/vercel.json задаёт команды npx с точной версией pnpm и frozen lockfile. Добавлена инструкция docs/vercel.md для Root Directory=web, server-side API_URL и повторного deployment актуального коммита. Значения секретов не менялись.
- Review: чистая установка завершилась с pnpm 10.34.5; существующий lockfile принят без перегенерации, SHA-256 исходной и проверочной копий совпал. Production Next.js build и TypeScript прошли. Проверка выполнена на Windows Node.js 22.21.1 в .local/qa/vercel-pnpm10, а не на Vercel; статус облачного deployment этим не подтверждается.
- Correction: прежнее указание полагаться только на ENABLE_EXPERIMENTAL_COREPACK было недостаточным. Теперь сборка выбирает pnpm явно. Адрес 127.0.0.1:8402 в npm-логе не связан с API_URL фермы. Точная версия pnpm первого неудачного deployment отсутствует в предоставленном логе.
- Tools: Git, PowerShell, npm/npx/pnpm, Next.js build, официальная документация Vercel; дополнительных агентов и новых внешних ручных настроек не было.

## 2026-09-10 — совместимость Next.js standalone и Vercel

- User review: новый лог ef42c97 подтвердил исправление pnpm, затем показал ENOENT .next/next-server.js.nft.json после onBuildComplete адаптера Vercel.
- Plan/implementation: подтверждена известная проблема Next.js 16.3 (vercel/next.js#96646); standalone отключён только при VERCEL=1. Docker сохраняет прежнюю упаковку. Фиктивный nft-файл не создаётся, версии Next.js/React не меняются.
- Agent review: локальная production-сборка с VERCEL=1 и TypeScript успешны, standalone/server.js отсутствует. Повтор без VERCEL успешен, standalone/server.js создан для Docker. Это проверка двух ветвей конфигурации на Windows, не выполнение облачного адаптера; результат нового Vercel deployment проверяется отдельно через GitHub status.
- Tools: PowerShell, Git, apply_patch, pnpm, Next.js build, исходники установленного Next.js и issue в официальном репозитории; дополнительные агенты не использовались.

## 2026-09-10 — вход и самостоятельная регистрация

- Request: убрать вход по выбранной роли; вход по почте или логину и паролю. Уточнение пользователя: нужна также самостоятельная регистрация.
- Plan: отдельные личные аккаунты, сохранение прежних principals/данных/паролей, миграция без переписывания старых SQL, новая форма и проверки изоляции. Изменение масштаба отражено у владельца правил в AGENTS A1/A7 и в PF-37-002; специализированные процедуры не дублировались. МастерПромпт.md отсутствует в рабочем дереве, исходник не восстанавливался по памяти.
- Implementation: accounts + миграция 008, одноразовый импорт admin/demo из прежних credentials, scrypt-хеши, уникальные логины/почта, регистрация в транзакции с сессией, вход по нормализованному идентификатору, CSRF и лимиты попыток. API не принимает роль и не возвращает хеш. AuthForm заменяет select; Workspace размонтируется при выходе. Старые интеграционные скрипты используют admin/demo.
- Agent review: 38 unittest passed; integration_auth в отдельной временной PostgreSQL DB проверил миграцию старой схемы, повтор миграции, старую сессию/идеи/пароли, регистрацию, оба входа, дубликаты, 401/403/404/422/429, отсутствие orphan principals после конфликта и секретов в audit. Первый запуск pytest ошибочно собрал импортированную функцию test_program как тест с fixture; штатный unittest discover выполнил все 38 тестов успешно. Внешние ИИ-вызовы не выполнялись.
- Browser review: Playwright/Edge headless, Browser plugin not available; использован уже установленный runtime. http://localhost:3000, 1440×1000 и 390×844: нет выбора роли/пустого экрана/overlay; регистрация, несовпадение паролей, пустое пространство, reload, logout, ошибка входа, вход по почте в верхнем регистре и логину прошли. JavaScript exceptions нет; ожидаемые auth 401 и существующий favicon.ico 404 разобраны отдельно. Снимки .local/qa/auth-login-desktop.png и auth-register-mobile.png просмотрены. Технические аккаунты после проверки удалены, пользовательские данные не затронуты.
- Operations/correction: новые Docker bind mounts первоначально отказали с input/output error. До восстановления сохранён приватный pg_dump .local/qa/before-auth-20260910.dump. Обычный restart Docker завис, выполнено завершение только процессов Docker и перезапуск docker-desktop WSL/приложения. Работающие контейнеры восстановлены, API/Web обновлены. До и после миграции 7 идей; checksum ID/owner/version совпал. Живой вход admin со старым паролем успешен. Production-сборки Docker standalone и VERCEL=1 прошли вместе с TypeScript.
- Boundaries: почтового подтверждения, восстановления и удаления аккаунта нет; старая почта не выдумана, admin/demo входят по логину. Новых секретов или ручных внешних настроек не требуется. Cloudflare API_URL не изменялся. Исходники отправляются в уже разрешённый GitHub main, статус Vercel проверяется отдельно.
- Tools/models: текущий Codex, PowerShell, apply_patch, Docker Compose/WSL, Python unittest/httpx/TestClient/PostgreSQL, Next.js/pnpm, Playwright Edge, Git/GitHub status. Skills product-farm-requirements, react-best-practices и frontend-testing-debugging. Дополнительные агенты не использовались.

## 2026-09-10 — недоступность регистрации через Cloudflare

- Request: при регистрации опубликованный Web сообщает «Сервер недоступен. Проверьте запуск API и PostgreSQL».
- Diagnosis: API/PostgreSQL healthy, localhost:8000/health и localhost:3000/api/health — 200. Старый cloudflared процесс запущен, но /ready возвращает 503, readyConnections=0; зарегистрировано 767 server_error при registerConnection, старый hostname не разрешается. Процесс сам по себе не доказывает работу туннеля. Vercel deployment закрыт Deployment Protection, подключённый Vercel инструмент возвращает teams=[], доступа к переменным проекта нет.
- Correction: запущен новый Quick Tunnel с HTTP/2 на тот же localhost:8000, metrics 127.0.0.1:20242, скрытый процесс; /ready=200 и readyConnections=1. Старый неработающий процесс завершён после проверки нового. Через публичный HTTPS успешно выполнены health=200, регистрация=201, проверка сессии, пустая воронка, вход по почте и выход; технический аккаунт очищен. Пользователю передан проверенный новый адрес для API_URL и запрошены сохранение переменной/Redeploy в его Vercel-проекте; подтверждение пока не получено.
- Code: proxy отдельно сообщает API_URL_MISSING в Vercel и сетевую недоступность API/туннеля, записывает только безопасный код/метод без credentials, cookie, body и URL. Пробелы и завершающий слеш API_URL нормализуются. Ошибка транспорта больше не представляется как доказанный отказ PostgreSQL.
- Verification: production Docker build и TypeScript прошли; два отдельных временных контейнера проверили POST с отсутствующим API_URL и отказом соединения — 503/API_URL_MISSING и 503/ECONNREFUSED; безопасные логи проверены, контейнеры удалены. Рабочий Web обновлён, /api/health=200.
- Tools: PowerShell, Docker, HTTP/httpx, cloudflared, Vercel read-only connector, CUA inventory, официальные источники Cloudflare/Vercel; skill investigation-mode. Изменений ключей и внешних сообщений третьим лицам нет; новый туннель открыт в пределах ранее разрешённой пользователем схемы публикации.

## 2026-09-10 — замечание пользователя об автономности

- User review: пользователь ожидает ввод только идеи; подготовка/исследование/варианты/проверка относятся к ферме, последующие правки — по желанию пользователя.
- Code review: IdeaEditor требует отдельного сохранения и кнопки structure; Research требует загрузки JSON и ручных variant_prompts; StartRun содержит обязательный dataset_version_id и создаёт варианты до агентного исследования. Worker стартует только после этой подготовки; experiments и начальные промпты ориентированы на классификацию обращений.
- Assessment: реализована техническая цепочка после ручной подготовки, но исходный сценарий A1/A6 «идея → автономная работа» не завершён. Технические интеграционные тесты на заранее подготовленных fixtures не являются его приёмкой. Это недоработка реализации, а не обоснованное изменение пользовательских требований.
- Required correction: серверный процесс приёма идеи и самостоятельной подготовки/планирования, исследование без обязательного предварительного dataset, агентный выбор вариантов/метрик/плана эксперимента, один входной экран и результаты с необязательными правками. Неизвестные сведения отделять от допущений; реальные наблюдения и разрешения запрашивать адресно, не выдавать синтетические данные за измерения. Масштаб исправления требует изменения workflow/API/модели, а не только скрытия полей. Реализация этой переработки в текущем диагностическом срезе ещё не выполнена.

## 12 сентября 2026 — RouterAI / v1.3

Request: применить PDF локально → Git → Vercel; очистить ненужные файлы. Лимит пользователя: 20 ₽/исследование. Plan: единый адаптер, двухвызовный поиск и citations, расходы, очистка, публикация. Implementation: ProviderProfile/политики v3, Qwen и независимый Critic, STT, атомарные резервы и usage, JSON repair из сохранённых citations, UI расходов/источников. Tools: terminal, локальные skills, официальная документация RouterAI; платные модельные запросы не запускались. Review: тесты запрещены пользователем; функциональная проверка не заявляется. Correction: удалён прежний backend и отменённые инструменты миграции, ключи оставлены вне Git. Рекурсивная очистка кэшей заблокирована автоматической проверкой; применены отдельные удаления отслеживаемых исходников через патч, кэши сохранены.

## 12 сентября 2026 — Продуктовый редизайн и ориентированный на пользователя интерфейс

Request: глубокая переработка дизайна без фич для программистов, фокус на ценности для бизнеса, больше визуальных акцентов (Arounda-стиль), единый скроллируемый дашборд (Вариант А), готовые шаблоны-подсказки, запись с микрофона в браузере и удаление идей.
Plan: implementation_plan.md одобрен пользователем; внедрение дизайн-токенов, SVG-иконок, MetricCard, VerdictBanner, VoiceRecorder (MediaRecorder API), интерактивного демо MVP, якорной навигации отчёта и скрытия технического паспорта за аккордеон.
Implementation:
- globals.css: полноценная дизайн-система в стиле Arounda (светлые поверхности, янтарный акцент #F5D65C, мягкие тени, анимации, адаптивность).
- UI.tsx: векторные иконки, статусные бейджи, карточки метрик, баннер продуктового вердикта, таймер и запись с микрофона, модальное окно удаления идей.
- CloudFarm.tsx: карточки гипотез, поиск, шаблоны быстрого старта, пошаговый трекер исследования (Stepper), скроллируемый дашборд отчёта с якорной навигацией (#verdict, #economics, #variants, #market, #risks, #sources, #decision).
- MvpRenderer.tsx: двухколоночная песочница интерактивного прототипа.
- AuthForm.tsx: карточный интерфейс входа/регистрации с сегментированным переключателем.
- api.ts: добавлена функция `del` для удаления идей через существующий эндпоинт DELETE /api/ideas/:id.
Verification: npx tsc --noEmit и npx next build успешно завершены без ошибок (код 0).

## 2026-09-12: Исправление распознавания аудиозаписи с микрофона
- storage.ts: добавлена функция `normalizeAudioMime` для очистки параметров кодеков (например, `audio/webm;codecs=opus`), расширен список допустимых MIME-типов и заменено выбрасывание базовой `Error` на `ApiError(400, ...)` с понятным сообщением.
- api.ts: улучшена обработка ошибок сохранения аудиофайла и передачи в хранилище с детальным сообщением вместо скрывающего 503.
- CloudFarm.tsx: отправка чистого MIME-типа и расширения файла при завершении голосовой записи с микрофона.
Verification: сборка Next.js и проверка типов прошли без ошибок (код 0).

### 2026-09-12: Устранение ошибок «RouterAI: Ответ пуст или обрезан»
- policy.json: увеличен лимит токенов генерации `budget.maxTokens` с 4500 до 8192 для предотвращения преждевременного обрезания ответов сложных шагов (`report_editor`, `efficiency_analyst`, `strategist`, `market_researcher`).
- providers.ts: разделена диагностика ошибок RouterAI completion (`finish_reason === 'length'` теперь явно указывает «Ответ превысил лимит токенов (обрезан)», а при отсутствии текста проверяется `choice.text`, `choice.annotations` и непустой триммированный контент; пустые ответы отделены от обрезаний).
- prompts.ts: добавлены указания на ёмкость и лаконичность формулировок в системные промпты ролей для защиты от избыточного расхода токенов и контекста.
- Verification: чистая проверка типов `npx tsc --noEmit` и полная production-сборка `npx next build` завершены успешно (код 0).

### 2026-09-12: Продуктовый редизайн, продающий лендинг и улучшение UX
- LandingPage.tsx: создан презентабельный экран для неавторизованных пользователей с Hero-блоком, превью аналитического дашборда, 6 шагами пайплайна исследования, ключевыми ценностями и модальным окном авторизации/регистрации.
- CloudFarm.tsx:
  - Сайдбар со списком идей зафиксирован (sticky, top 64px, calc(100vh - 64px)) с внутренним скроллом, устраняя пустое пространство слева при прокрутке отчёта.
  - Блок параметров идеи (название и приоритет) сделан явным, открытым и привлекательным: крупное поле ввода названия и интерактивные чипсы-кнопки выбора приоритета (Низкий, Обычный, Высокий) взамен скрытого тега details.
  - Вся терминология в пользовательском интерфейсе («гипотеза») заменена на «идея».
- docs/page.tsx: страница «Как это работает» полностью переработана из монотонного текста в структурированный гид с карточками этапов, инфографикой, объяснением расчётов и техническими гарантиями.
- globals.css: добавлены стили продающего лендинга, чипсов приоритета, гайда документации и оптимизирована прокрутка сайдбара.
- UI.tsx, MvpRenderer.tsx, SharedReport.tsx, AuthForm.tsx: обновлены заголовки и тексты для единой терминологии «идея».
- Verification: чистая проверка типов `npx tsc --noEmit` и полная production-сборка `npx next build` завершены успешно (код 0).

### 2026-09-12: Исправление отображения модального окна входа и регистрации
- AuthForm.tsx: встроен элегантный внутренний крестик закрытия `.auth-card-close` прямо в верхний угол карточки, убрана внешняя серая подложка `.auth-page` при модальном отображении.
- LandingPage.tsx: убран дублирующийся внешний контейнер с крестиком, теперь модальное окно рендерит единую аккуратную карточку без эффекта «окно поверх окна».
- globals.css: настроены стили `.auth-card-close` и мягкая фоновая тень для карточки в модальном режиме.
- Verification: чистая проверка типов `npx tsc --noEmit` и полная production-сборка `npx next build` завершены успешно (код 0).

### 2026-09-12: Оптимизация формы создания идеи (UX и эргономика)
- CloudFarm.tsx: перестроен порядок формы заведения идеи:
  - Основное текстовое поле ввода «Суть идеи и сценарий» и панель голосового ввода/загрузки аудио теперь расположены первыми и сразу доступны пользователю без необходимости прокрутки страницы.
  - Блок параметров («Название и приоритет») перемещён ниже как вторичный шаг оформления и сделан компактным.
- globals.css: уменьшена высота и отступы блока `.farm-params-card` (до 12px 16px), уменьшены размеры полей ввода и чипсов приоритета, оптимизирован вертикальный ритм всей формы.
- Verification: чистая проверка типов `npx tsc --noEmit` и полная production-сборка `npx next build` завершены успешно (код 0).


